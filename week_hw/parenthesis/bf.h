#include <iostream>
#include <vector>
#include <math.h>
#include <stdio.h>
#include <set> //for testing (should I include stl stuff only in header?)
using namespace std;


vector<string> genAll(const int &n);

bool valid(string x);

vector<string> bf_paren(const int &n);
