#include <iostream>
#include <utility>
#include <string>
using namespace std;

int fib_index(int n){
  int index = 1;
  int a = 1;
  int b = 1;
  while(true){
    if(to_string(a).length() >= 
    swap(a, b);
    a += b;
    ++index;
  }
}

int main(){
  
}
